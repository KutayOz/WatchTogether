{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "localhost",
  "MongoDB": {
    "ConnectionString": "mongodb://localhost:27017",
    "DatabaseName": "watchtogether"
  },
  "Jwt": {
    "//": "Secret must be set via user-secrets or env var — never commit a real value.",
    "//Set with": "dotnet user-secrets set Jwt:Secret \"<run: openssl rand -base64 48>\"",
    "Secret": "__SET_VIA_USER_SECRETS_OR_ENV__"
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:5173",
      "http://localhost:5174"
    ]
  }
}
