{
  "name": "watchtogether-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply watchtogether --local",
    "db:migrate:remote": "wrangler d1 migrations apply watchtogether --remote"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.2",
    "hono": "^4.6.14",
    "jose": "^6.2.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@types/node": "^26.1.2",
    "typescript": "~5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.124.0"
  }
}
